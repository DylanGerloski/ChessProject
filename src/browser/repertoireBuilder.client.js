'use strict';

/**
 * PLACEHOLDER esbuild entry point for /repertoire-builder.html's bundle
 * (repertoire-builder.js). The follow-on task that builds the real
 * Repertoire Builder v1 (spec section 3.1) replaces
 * this file's contents entirely -- src/buildStatic.js's
 * buildRepertoireBuilderBundle() call (bundleBrowserEntry pointed at THIS
 * path) never changes, so that task never touches buildStatic.js. See
 * src/renderRepertoireBuilder.js's header comment for the full reasoning.
 *
 * Deliberately does nothing yet -- the placeholder page has no interactive
 * element for this to wire up.
 */

console.log('Repertoire builder: coming soon.');
