/* Auto-generated by src/buildStatic.js (esbuild) from
 * src/browser/playerLookup.client.js and its module dependencies
 * (src/fetchLichess.js, src/process.js, src/render.js). Do not edit
 * this file directly -- edit the source files above and re-run `node
 * src/buildStatic.js`.
 *
 * This calls only Lichess's keyless public API (https://lichess.org/api)
 * directly from your browser. No Lichess API token is used, read, or present
 * anywhere in this file. */
"use strict";(()=>{var __getOwnPropNames=Object.getOwnPropertyNames;var __commonJS=(cb,mod)=>function(){try{return mod||(0,cb[__getOwnPropNames(cb)[0]])((mod={exports:{}}).exports,mod),mod.exports}catch(e){throw mod=0,e}};var require_fetchLichess=__commonJS({"src/fetchLichess.js"(exports,module){"use strict";var BASE_URL="https://lichess.org",LichessNotFoundError=class extends Error{constructor(username){super(`Lichess user not found: ${username}`),this.name="LichessNotFoundError",this.username=username}},LichessRateLimitError=class extends Error{constructor(retryAfter){super(`Lichess API rate limit hit${retryAfter?` (retry after ${retryAfter}s)`:""}`),this.name="LichessRateLimitError",this.retryAfter=retryAfter||null}},LichessApiError=class extends Error{constructor(status,statusText,url){super(`Lichess API request failed: ${status} ${statusText} (${url})`),this.name="LichessApiError",this.status=status}};async function handleErrors(response,url,username){if(response.status===404)throw new LichessNotFoundError(username);if(response.status===429){let retryAfter=response.headers&&typeof response.headers.get=="function"?response.headers.get("retry-after"):null;throw new LichessRateLimitError(retryAfter)}if(!response.ok)throw new LichessApiError(response.status,response.statusText,url)}async function fetchUserProfile(username,{fetchImpl=fetch}={}){let url=`${BASE_URL}/api/user/${encodeURIComponent(username)}`,response=await fetchImpl(url,{headers:{Accept:"application/json"}});return await handleErrors(response,url,username),response.json()}async function fetchRatingHistory(username,{fetchImpl=fetch}={}){let url=`${BASE_URL}/api/user/${encodeURIComponent(username)}/rating-history`,response=await fetchImpl(url,{headers:{Accept:"application/json"}});return await handleErrors(response,url,username),response.json()}async function fetchRecentGames(username,{max=10,fetchImpl=fetch}={}){let url=`${BASE_URL}/api/games/user/${encodeURIComponent(username)}?max=${encodeURIComponent(max)}&moves=false`,response=await fetchImpl(url,{headers:{Accept:"application/x-ndjson"}});return await handleErrors(response,url,username),(await response.text()).split(`
`).map(line=>line.trim()).filter(line=>line.length>0).map(line=>JSON.parse(line))}module.exports={BASE_URL,fetchUserProfile,fetchRatingHistory,fetchRecentGames,LichessNotFoundError,LichessRateLimitError,LichessApiError}}});var require_process=__commonJS({"src/process.js"(exports,module){"use strict";function summarizeRatingHistory(history){return Array.isArray(history)?history.map(variant=>{let points=Array.isArray(variant.points)?variant.points:[];if(points.length===0)return{variant:variant.name,current:null,peak:null,low:null,gamesRecorded:0,firstDate:null,lastDate:null,change:null};let ratings=points.map(p=>p[3]),first=points[0],last=points[points.length-1];return{variant:variant.name,current:last[3],peak:Math.max(...ratings),low:Math.min(...ratings),gamesRecorded:points.length,firstDate:pointToDateString(first),lastDate:pointToDateString(last),change:last[3]-first[3]}}).filter(row=>row.gamesRecorded>0):[]}function pointToDateString([year,month0,day]){let mm=String(month0+1).padStart(2,"0"),dd=String(day).padStart(2,"0");return`${year}-${mm}-${dd}`}function summarizeGames(games,username){if(!Array.isArray(games)||!username)return{totalGames:0,wins:0,losses:0,draws:0,winRate:null,avgOpponentRating:null,results:[]};let lowerUsername=username.toLowerCase(),wins=0,losses=0,draws=0,opponentRatingSum=0,opponentRatingCount=0,results=games.map(game=>{let players=game.players||{},white=players.white||{},black=players.black||{},isWhite=(white.user&&white.user.name||"anonymous").toLowerCase()===lowerUsername,me=isWhite?white:black,opponent=isWhite?black:white,result;game.winner?game.winner===(isWhite?"white":"black")?(result="win",wins+=1):(result="loss",losses+=1):(result="draw",draws+=1);let opponentRating=typeof opponent.rating=="number"?opponent.rating:null;return opponentRating!==null&&(opponentRatingSum+=opponentRating,opponentRatingCount+=1),{opponent:opponent.user&&opponent.user.name||"Anonymous",opponentRating,result,color:isWhite?"white":"black",variant:game.variant||"standard",speed:game.speed||"unknown",date:game.createdAt?new Date(game.createdAt).toISOString().slice(0,10):null}}),totalGames=games.length;return{totalGames,wins,losses,draws,winRate:totalGames>0?Number((wins/totalGames*100).toFixed(1)):null,avgOpponentRating:opponentRatingCount>0?Math.round(opponentRatingSum/opponentRatingCount):null,results}}module.exports={summarizeRatingHistory,summarizeGames}}});var require_render=__commonJS({"src/render.js"(exports,module){"use strict";function escapeHtml(str){return String(str).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#39;")}function formatPct(n){return typeof n=="number"?n.toFixed(1):"-"}var DESIGN_TOKENS={"--font-sans":'-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif',"--font-serif":'"Fraunces Variable", Georgia, "Iowan Old Style", "Palatino Linotype", "Times New Roman", serif',"--color-bg":"#f5f1e6","--color-surface":"#ffffff","--color-surface-alt":"#ece3cd","--color-text":"#23271f","--color-muted":"#5e5c4b","--color-accent":"#3c6e52","--color-accent-dark":"#2a4d3a","--color-accent-contrast":"#f8f5ea","--color-hover":"rgba(60, 110, 82, 0.08)","--color-focus":"#a35110","--color-border":"#ddd0ac","--color-border-strong":"#8a7b54","--color-win":"#2f7d43","--color-win-text":"#256034","--color-win-bg":"#e3f2e6","--color-draw":"#93731c","--color-draw-text":"#6f5714","--color-draw-bg":"#f4ecd2","--color-loss":"#b23b30","--color-loss-text":"#96302a","--color-loss-bg":"#fbe4e0","--color-board-light":"#ece3cd","--color-board-dark":"#c2ad82","--color-row-tint":"rgba(35, 39, 31, 0.035)","--text-xs":"0.75rem","--text-sm":"0.875rem","--text-base":"1rem","--text-md":"1.125rem","--text-lg":"1.375rem","--text-xl":"1.75rem","--text-2xl":"clamp(2rem, 1.55rem + 1.9vw, 2.75rem)","--leading-tight":"1.15","--leading-snug":"1.3","--leading-normal":"1.6","--leading-relaxed":"1.75","--weight-regular":"400","--weight-medium":"600","--weight-bold":"700","--measure":"68ch","--width-page":"880px","--width-wide":"1120px","--space-1":"0.25rem","--space-2":"0.5rem","--space-3":"0.75rem","--space-4":"1rem","--space-5":"1.5rem","--space-6":"2rem","--space-7":"3rem","--space-8":"4rem","--radius-sm":"6px","--radius-md":"10px","--radius-lg":"16px","--radius-pill":"999px","--shadow-sm":"0 1px 2px rgba(35, 39, 31, 0.08)","--shadow-md":"0 8px 24px rgba(35, 39, 31, 0.10)","--shadow-focus":"0 0 0 3px rgba(60, 110, 82, 0.25)","--border-hairline":"1px","--border-control":"2px","--motion-duration-fast":"150ms","--motion-duration-piece":"200ms","--motion-easing-standard":"cubic-bezier(0.4, 0, 0.2, 1)"};function designTokensCss(tokens){return Object.entries(tokens).map(([name,value])=>`    ${name}: ${value};`).join(`
`)}var SITE_CSS=`
  :root {
${designTokensCss(DESIGN_TOKENS)}
  }

  /* Self-hosted display face for headings only (--font-serif above). One
     variable woff2, wght+opsz axes, latin subset, ~66KB -- served from this
     same origin (never Google Fonts directly, so there's no third-party
     render-blocking stylesheet and no visitor IP sent off-site), preloaded
     in renderDocumentHead() with crossorigin. font-display: swap means
     headings render in the Georgia fallback immediately and swap in once
     the woff2 arrives, so there's no invisible-text flash. License: SIL OFL
     1.1, assets/fonts/FRAUNCES-OFL-LICENSE.txt. */
  @font-face {
    font-family: 'Fraunces Variable';
    font-style: normal;
    font-display: swap;
    font-weight: 100 900;
    src: url('/fonts/fraunces-variable.woff2') format('woff2-variations');
  }

  * { box-sizing: border-box; }

  .sr-only {
    position: absolute;
    width: 1px; height: 1px;
    padding: 0; margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  html { background: var(--color-bg); }

  body {
    font-family: var(--font-sans);
    background: var(--color-bg);
    color: var(--color-text);
    max-width: var(--width-page);
    margin: 0 auto;
    padding: var(--space-5) var(--space-4) var(--space-7);
    line-height: var(--leading-normal);
    font-size: var(--text-base);
  }

  /* Opt-in wide container for the three data-dense page types (repertoire
     band pages, the drill, player lookup) \u2014 added at those specific call
     sites only, never as the default. See design-standards.md 4.5. */
  body.layout--wide { max-width: var(--width-wide); }

  main { display: block; }

  /* Prose measure (design-standards.md 4.5 / P3): caps reading-line length
     to var(--measure) for running text inside main. Tables, boards, and
     .table-scroll are exempt by not matching this selector at all. The
     repertoire tree's <li> rows are data rows (move chips, WDL bars), not
     prose sentences, so they're excluded explicitly below \u2014 constraining
     them would fight the wide layout those pages just opted into. */
  main p, main li, main .subtitle, main blockquote {
    max-width: var(--measure);
  }
  main .repertoire-tree li { max-width: none; }

  a { color: var(--color-accent-dark); }
  a:hover { color: var(--color-accent); }

  :focus-visible {
    outline: 3px solid var(--color-focus);
    outline-offset: 2px;
    border-radius: var(--radius-sm);
  }

  .site-header {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-3);
    padding-bottom: var(--space-4);
    margin-bottom: var(--space-5);
    border-bottom: 2px solid var(--color-border);
  }

  .brand {
    font-family: var(--font-serif);
    font-weight: var(--weight-bold);
    font-size: var(--text-md);
    color: var(--color-accent-dark);
    letter-spacing: 0.01em;
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
  }

  .brand-mark {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.6em;
    height: 1.6em;
    border-radius: var(--radius-pill);
    background: var(--color-accent-dark);
    color: var(--color-accent-contrast);
    font-size: 0.95em;
  }

  .site-nav { display: flex; gap: var(--space-2); flex-wrap: wrap; }

  .site-nav a {
    color: var(--color-text);
    text-decoration: none;
    font-size: var(--text-sm);
    font-weight: 600;
    padding: var(--space-3) var(--space-3);
    border-radius: var(--radius-sm);
    transition: background-color 120ms ease, color 120ms ease;
  }

  .site-nav a:hover { background: var(--color-hover); color: var(--color-accent-dark); }
  .site-nav a[aria-current="page"] { background: var(--color-accent-dark); color: var(--color-accent-contrast); }

  h1, h2, h3 { font-family: var(--font-serif); color: var(--color-accent-dark); line-height: var(--leading-snug); text-wrap: balance; }
  h1.page-title { font-size: var(--text-2xl); line-height: var(--leading-tight); margin: 0; }
  h2 { font-size: var(--text-lg); margin: var(--space-6) 0 var(--space-3); }
  h3 { font-size: var(--text-md); margin: var(--space-5) 0 var(--space-2); }

  /* Vertical rhythm (design-standards.md 4.5): section spacing opens up at
     tablet width and above; stays tighter on mobile (the --space-6 default
     set on h2 just above). */
  @media (min-width: 768px) {
    h2 { margin-top: var(--space-8); }
  }

  /* Progressive enhancement, zero cost where unsupported: avoids
     single-word orphan lines in body copy. */
  p { text-wrap: pretty; }

  .subtitle { color: var(--color-muted); margin: var(--space-2) 0 0; font-size: var(--text-base); }

  .empty-note {
    color: var(--color-muted);
    background: var(--color-surface-alt);
    border: 1px dashed var(--color-border);
    border-radius: var(--radius-md);
    padding: var(--space-4);
  }

  .table-hint {
    display: none;
    font-size: var(--text-xs);
    color: var(--color-muted);
    margin: 0 0 var(--space-2);
  }

  .table-scroll {
    overflow-x: auto;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-sm);
    margin: var(--space-3) 0 var(--space-6);
    background: var(--color-surface);
  }

  table {
    width: 100%;
    min-width: 480px;
    border-collapse: collapse;
    font-size: var(--text-sm);
  }

  /* Table header style: a transparent thead with a tracked label over a
     2px accent bottom rule, replacing the old inverted solid
     --color-accent-dark bar, which read as a dated admin-template look. */
  thead th {
    text-align: left;
    padding: var(--space-3) var(--space-4);
    background: transparent;
    color: var(--color-muted);
    font-weight: var(--weight-bold);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    white-space: nowrap;
    border-bottom: 2px solid var(--color-accent);
  }

  tbody td {
    padding: var(--space-3) var(--space-4);
    border-bottom: var(--border-hairline) solid var(--color-border);
  }

  tbody tr:last-child td { border-bottom: none; }
  /* Zebra striping replaced with the hairline row rule above; a very light
     tint (var(--color-row-tint), not full removal) keeps wide tables
     trackable at 200% zoom, verified manually against a repertoire table. */
  tbody tr:nth-child(even) td { background: var(--color-row-tint); }
  tbody tr:hover td { background: var(--color-hover); }

  /* Right-aligned numeric columns: tabular-nums keeps digits stacked
     instead of jittering column width row to row. Opt-in via class (not
     nth-child) since column layouts differ per table. */
  th.num, td.num { text-align: right; font-variant-numeric: tabular-nums; }

  tr.result-win td:first-child { box-shadow: inset 3px 0 0 var(--color-win); }
  tr.result-loss td:first-child { box-shadow: inset 3px 0 0 var(--color-loss); }
  tr.result-draw td:first-child { box-shadow: inset 3px 0 0 var(--color-draw); }

  .delta { font-weight: var(--weight-bold); }
  .delta--pos { color: var(--color-win-text); }
  .delta--neg { color: var(--color-loss-text); }
  .delta--zero { color: var(--color-muted); }

  .badge {
    display: inline-block;
    padding: 0.15em 0.6em;
    border-radius: var(--radius-pill);
    font-size: var(--text-xs);
    font-weight: var(--weight-bold);
    letter-spacing: 0.02em;
  }
  .badge--win { background: var(--color-win-bg); color: var(--color-win-text); }
  .badge--loss { background: var(--color-loss-bg); color: var(--color-loss-text); }
  .badge--draw { background: var(--color-draw-bg); color: var(--color-draw-text); }

  .summary-line {
    color: var(--color-muted);
    margin: var(--space-2) 0 var(--space-4);
  }

  .lookup-form {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
    margin: var(--space-4) 0 var(--space-6);
  }

  .lookup-form label {
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
    font-size: var(--text-sm);
    color: var(--color-muted);
  }

  .lookup-form input,
  .lookup-form select {
    flex: 1 1 240px;
    min-height: 44px;
    font: inherit;
    font-size: var(--text-base);
    padding: var(--space-3) var(--space-4);
    border: var(--border-control) solid var(--color-border-strong);
    border-radius: var(--radius-md);
    background: var(--color-surface);
    color: var(--color-text);
    transition: border-color 120ms ease, box-shadow 120ms ease;
  }

  .lookup-form input:hover,
  .lookup-form select:hover { border-color: var(--color-accent); }
  .lookup-form input:focus-visible,
  .lookup-form select:focus-visible {
    border-color: var(--color-accent);
    box-shadow: var(--shadow-focus);
    outline: none;
  }

  .lookup-form button {
    min-height: 44px;
    font: inherit;
    font-size: var(--text-base);
    font-weight: var(--weight-bold);
    padding: var(--space-3) var(--space-5);
    border: none;
    border-radius: var(--radius-md);
    background: var(--color-accent-dark);
    color: var(--color-accent-contrast);
    cursor: pointer;
    transition: background-color 120ms ease, transform 120ms ease;
  }

  .lookup-form button:hover { background: var(--color-accent); }
  .lookup-form button:active { transform: translateY(1px); }

  .status-message {
    border-radius: var(--radius-md);
    padding: var(--space-4) var(--space-5);
    margin: var(--space-3) 0 var(--space-6);
    font-size: var(--text-base);
  }

  .status-message--loading {
    background: var(--color-surface-alt);
    color: var(--color-muted);
    display: flex;
    align-items: center;
    gap: var(--space-3);
  }

  .status-message--loading::before {
    content: "";
    width: 1.1em;
    height: 1.1em;
    border-radius: var(--radius-pill);
    border: var(--border-control) solid var(--color-border);
    border-top-color: var(--color-accent);
    animation: spin 800ms linear infinite;
  }

  .status-message--error {
    background: var(--color-loss-bg);
    color: var(--color-loss);
    border: 1px solid var(--color-loss);
    font-weight: 600;
  }

  @keyframes spin { to { transform: rotate(360deg); } }

  .move-chip {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.1em 0.6em;
    border-radius: var(--radius-sm);
    font-family: var(--font-sans);
    font-weight: var(--weight-bold);
    font-size: var(--text-sm);
  }

  .move-chip--white {
    background: var(--color-surface);
    border: 1.5px solid var(--color-accent-dark);
    color: var(--color-accent-dark);
  }

  .move-chip--black {
    background: var(--color-accent-dark);
    border: 1.5px solid var(--color-accent-dark);
    color: var(--color-accent-contrast);
  }

  .wdl-bar {
    display: inline-flex;
    width: 110px;
    height: 8px;
    border-radius: var(--radius-pill);
    overflow: hidden;
    background: var(--color-border);
    vertical-align: middle;
  }

  .wdl-seg--win { background: var(--color-win); height: 100%; }
  .wdl-seg--draw { background: var(--color-draw); height: 100%; border-left: var(--border-hairline) solid var(--color-surface); }
  .wdl-seg--loss { background: var(--color-loss); height: 100%; border-left: var(--border-hairline) solid var(--color-surface); }

  /* Widened WDL bar for the one hero table per opening page
     (renderBandsTable's "How it scores at your rating"). Percentages stay
     visible alongside it (.wdl-label below), so color is never the sole
     encoding. */
  .wdl-bar--lg { width: 100%; min-width: 160px; height: 12px; }

  .wdl-label { font-size: var(--text-xs); color: var(--color-muted); }

  .repertoire-intro { color: var(--color-muted); margin: 0 0 var(--space-5); }

  ul.repertoire-tree, ul.repertoire-tree ul {
    list-style: none;
    margin: 0;
    padding-left: var(--space-5);
    border-left: 2px solid var(--color-border);
  }
  ul.repertoire-tree { padding-left: 0; border-left: none; }

  .repertoire-tree li { margin: var(--space-3) 0; }

  .rep-node-row {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-3);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: var(--space-2) var(--space-4);
    box-shadow: var(--shadow-sm);
  }

  .rep-games { font-size: var(--text-sm); color: var(--color-text); }
  .rep-pct { color: var(--color-muted); }
  .rep-rating { color: var(--color-muted); font-size: var(--text-xs); }

  /* ECO family/volume index pages (Phase 7d). An ECO code is metadata, not
     a call to action -- bordered, not accent-filled (design-standards.md:
     accent is reserved for the one primary action per view). */
  .eco-chip {
    display: inline-block;
    font-family: var(--font-sans);
    font-size: var(--text-xs);
    font-weight: var(--weight-bold);
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--color-accent-dark);
    background: var(--color-surface);
    border: 1px solid var(--color-border-strong);
    border-radius: var(--radius-sm);
    padding: 2px var(--space-2);
    margin-right: var(--space-2);
    white-space: nowrap;
  }
  .variation-group-label {
    font-weight: var(--weight-bold);
    color: var(--color-text);
    margin: var(--space-3) 0 var(--space-1);
  }
  .eco-index-table td.eco-names span + span::before { content: ", "; }

  .pagination {
    display: flex;
    justify-content: space-between;
    gap: var(--space-3);
    margin: var(--space-6) 0;
    padding-top: var(--space-4);
    border-top: 1px solid var(--color-border);
  }
  .pagination a {
    display: inline-flex;
    align-items: center;
    min-height: 44px;
    padding: var(--space-2) var(--space-4);
    border: 1.5px solid var(--color-border-strong);
    border-radius: var(--radius-md);
    text-decoration: none;
    font-weight: var(--weight-bold);
  }
  .pagination a:hover { background: var(--color-hover); }
  .pagination .pagination-status { color: var(--color-muted); font-size: var(--text-sm); align-self: center; }

  footer.site-footer {
    color: var(--color-muted);
    font-size: var(--text-xs);
    margin-top: var(--space-7);
    padding-top: var(--space-4);
    border-top: 1px solid var(--color-border);
  }

  .newsletter-signup {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: var(--space-4);
    margin-top: var(--space-4);
    max-width: 60ch;
  }
  .newsletter-heading {
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--color-text);
    margin: 0 0 var(--space-2);
  }
  .newsletter-description {
    color: var(--color-muted);
    font-size: var(--text-xs);
    margin: 0 0 var(--space-3);
  }
  .newsletter-signup--pending .newsletter-description { margin-bottom: 0; }
  .newsletter-fields {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }
  .newsletter-fields input[type="email"] {
    flex: 1 1 220px;
    min-height: 44px;
    padding: var(--space-2) var(--space-3);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    font-size: var(--text-sm);
    background: var(--color-bg);
    color: var(--color-text);
  }
  .newsletter-fields input[type="email"]:focus-visible {
    outline: 2px solid var(--color-focus);
    outline-offset: 2px;
  }
  .newsletter-fields button {
    min-height: 44px;
    padding: var(--space-2) var(--space-4);
    border: 1px solid var(--color-accent);
    border-radius: var(--radius-sm);
    background: var(--color-accent);
    color: var(--color-accent-contrast);
    font-size: var(--text-sm);
    font-weight: 600;
    cursor: pointer;
  }
  .newsletter-fields button:hover { background: var(--color-accent-dark); }
  .newsletter-fields button:focus-visible {
    outline: 2px solid var(--color-focus);
    outline-offset: 2px;
  }

  .support-links {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
    margin-top: var(--space-3);
  }
  .support-links a {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-3);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    color: var(--color-accent-dark);
    text-decoration: none;
    background: var(--color-surface);
  }
  .support-links a:hover {
    background: var(--color-hover);
    border-color: var(--color-accent);
  }

  .disclosure-note {
    color: var(--color-muted);
    font-size: var(--text-xs);
    margin: var(--space-3) 0 0;
    max-width: 60ch;
  }

  .legal-links {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
    margin-top: var(--space-3);
    font-size: var(--text-xs);
  }
  .legal-links a { color: var(--color-muted); }
  .legal-links a:hover { color: var(--color-accent-dark); }

  .prose { max-width: var(--measure); }
  .prose p { margin: 0 0 var(--space-4); line-height: var(--leading-relaxed); }
  .prose h2 { margin-top: var(--space-6); }
  .prose ul, .prose ol { padding-left: var(--space-5); line-height: var(--leading-relaxed); }
  .prose blockquote {
    border-left: 3px solid var(--color-accent);
    padding-left: var(--space-4);
    color: var(--color-muted);
    font-family: var(--font-sans);
    margin: var(--space-5) 0;
  }

  .breadcrumb { font-size: var(--text-xs); color: var(--color-muted); margin-bottom: var(--space-3); }
  .breadcrumb a { color: var(--color-muted); }
  .breadcrumb ol { list-style: none; margin: 0; padding: 0; display: flex; flex-wrap: wrap; gap: var(--space-2); }
  .breadcrumb li { display: inline; }
  .breadcrumb .breadcrumb-sep { color: var(--color-border); }

  .article-meta {
    font-size: var(--text-xs);
    color: var(--color-muted);
    border-bottom: 1px solid var(--color-border);
    padding-bottom: var(--space-3);
    margin: var(--space-3) 0 var(--space-5);
  }

  .toc {
    background: var(--color-surface-alt);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: var(--space-4) var(--space-5);
    margin: 0 0 var(--space-6);
    font-size: var(--text-sm);
  }

  .card-grid {
    display: grid;
    gap: var(--space-4);
    margin: var(--space-4) 0 var(--space-6);
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
  }

  .card {
    position: relative;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: var(--space-4);
    box-shadow: var(--shadow-sm);
    transition: box-shadow 120ms ease, transform 120ms ease;
  }
  .card:hover { box-shadow: var(--shadow-md); transform: translateY(-1px); }
  .card h3 { margin: 0 0 var(--space-2); font-size: var(--text-base); }
  .card p { margin: 0; font-size: var(--text-sm); color: var(--color-muted); }
  .card a { text-decoration: none; }
  .card a::after { content: ""; position: absolute; inset: 0; }

  /* One primary action per view (design-standards.md hierarchy rule): the
     repertoire-band selector is the homepage's single
     accent-filled primary action. Every other homepage CTA (the drill card,
     the openings cards) is demoted to an outline card \u2014 same link targets,
     lower visual weight. Modifier classes only; markup/link targets
     unchanged. */
  .card--primary {
    background: var(--color-accent-dark);
    border-color: var(--color-accent-dark);
  }
  .card--primary h3, .card--primary h3 a { color: var(--color-accent-contrast); }
  .card--primary p, .card--primary p a { color: var(--color-accent-contrast); }
  .card--primary p a:hover { color: var(--color-accent-contrast); text-decoration: underline; }

  .card--outline {
    background: transparent;
    box-shadow: none;
  }
  .card--outline:hover { box-shadow: none; transform: none; border-color: var(--color-accent); }

  /* The single overloaded .card rule split by content shape (orthogonal to
     the primary/outline visual-weight axis above). card--nav is a pure
     navigational link (drill CTA, related openings, guides hub; no data
     carried); card--stat additionally carries inline WDL data on opening
     entry-page cards, so a visitor never has to click through to see
     whether an opening is worth their time. */
  .card--nav p { margin: 0; }

  .card--stat .card-wdl-row { display: flex; align-items: center; gap: var(--space-2); margin: var(--space-2) 0; }
  .card--stat .card-score { margin: 0; font-size: var(--text-sm); font-weight: var(--weight-bold); color: var(--color-accent-dark); }
  .card--stat.card--primary .card-score,
  .card--stat.card--outline .card-score { color: inherit; }

  /* The four rating-band pickers as one role=group control with 44px
     pill links, replacing four floating cards that carried the same visual
     weight as unrelated nav cards elsewhere on the page. */
  .band-picker {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
    margin: var(--space-4) 0 var(--space-6);
  }
  .band-pill {
    display: inline-flex;
    align-items: center;
    gap: var(--space-1);
    min-height: 44px;
    padding: var(--space-2) var(--space-5);
    border-radius: var(--radius-pill);
    background: var(--color-accent-dark);
    color: var(--color-accent-contrast);
    text-decoration: none;
    font-weight: var(--weight-bold);
    font-size: var(--text-sm);
    transition: background-color 120ms ease, transform 120ms ease;
  }
  .band-pill:hover { background: var(--color-accent); color: var(--color-accent-contrast); transform: translateY(-1px); }
  .band-pill-color { font-weight: var(--weight-regular); opacity: 0.85; }

  /* Eyebrow label above an h1, shared by renderPageHead(). */
  .page-eyebrow {
    font-size: var(--text-xs);
    font-weight: var(--weight-bold);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-accent);
    margin: 0 0 var(--space-2);
  }

  .callout {
    background: var(--color-surface-alt);
    border-left: 4px solid var(--color-accent);
    border-radius: var(--radius-sm);
    padding: var(--space-4) var(--space-5);
    margin: var(--space-5) 0;
    font-size: var(--text-sm);
  }

  .stat-row { display: flex; flex-wrap: wrap; gap: var(--space-4); margin: var(--space-4) 0 var(--space-6); }
  .stat {
    flex: 1 1 140px;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: var(--space-4);
  }
  .stat-value { font-family: var(--font-sans); font-size: var(--text-xl); color: var(--color-accent-dark); line-height: 1.1; }
  .stat-label { font-size: var(--text-xs); color: var(--color-muted); text-transform: uppercase; letter-spacing: 0.04em; }

  .source-list { font-size: var(--text-sm); color: var(--color-muted); }

  .board {
    display: grid;
    grid-template-columns: repeat(8, 1fr);
    width: min(100%, 352px);
    aspect-ratio: 1 / 1;
    border: 2px solid var(--color-accent-dark);
    border-radius: var(--radius-sm);
    overflow: hidden;
    margin: 0;
  }
  .board-sq {
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: clamp(18px, 5.2vw, 30px);
    line-height: 1;
    font-family: "Segoe UI Symbol", "Apple Symbols", "Noto Sans Symbols 2", "DejaVu Sans", sans-serif;
  }
  .board-sq--light { background: var(--color-board-light); }
  .board-sq--dark { background: var(--color-board-dark); }
  .board-pc--w { color: var(--color-surface); text-shadow: 0 0 1px var(--color-text), 0 0 1px var(--color-text), 0 0 2px var(--color-text); }
  .board-pc--b { color: var(--color-text); }
  /* Phase 7c: real Cburnett SVG piece artwork (src/boardSvg.js), replacing
     the .board-pc--w/--b Unicode-glyph technique above for the static
     content-page diagram. .board-pc--w/--b stay defined -- the Italian
     Game drill's own board (src/renderDrill.js) still uses them. */
  .board-piece { width: 84%; height: 84%; display: block; }
  figure.board-figure { margin: var(--space-4) 0 var(--space-6); }
  figcaption { font-size: var(--text-sm); color: var(--color-muted); margin-top: var(--space-2); }

  /* Interactive board component (src/boardWidget.js, Phase 7c) -- a
     tokenised theme for cm-chessboard, replacing its own shipped ".default"
     palette entirely (design-standards.md: "do not ship its palette").
     cm-chessboard has no CSS custom properties of its own (verified by
     reading node_modules/cm-chessboard/assets/chessboard.css -- every rule
     is a hardcoded hex scoped by a .default/.green/etc. theme class), so
     this defines a same-shaped theme class of our own, .repertoire-theme,
     wired to our tokens instead. boardWidget.js sets style.cssClass to
     "repertoire-theme" and never loads cm-chessboard's own chessboard.css. */
  .cm-chessboard.repertoire-theme .board .square.white { fill: var(--color-board-light); }
  .cm-chessboard.repertoire-theme .board .square.black { fill: var(--color-board-dark); }
  .cm-chessboard.repertoire-theme.border-type-thin .board .border { stroke: var(--color-accent-dark); stroke-width: 0.7%; fill: var(--color-accent-dark); }
  .cm-chessboard.repertoire-theme .coordinates .coordinate { font-size: 7px; cursor: default; }
  .cm-chessboard.repertoire-theme .coordinates .coordinate.black { fill: var(--color-board-light); }
  .cm-chessboard.repertoire-theme .coordinates .coordinate.white { fill: var(--color-board-dark); }
  .cm-chessboard .board.input-enabled .square { cursor: pointer; }
  .cm-chessboard .coordinates, .cm-chessboard .markers-layer, .cm-chessboard .pieces-layer, .cm-chessboard .markers-top-layer { pointer-events: none; }
  /* Shared focus-ring language (:focus-visible above), not cm-chessboard's
     own hardcoded #0066cc default. These two rects (drawn by the
     Accessibility extension's keyboardMoveInput -- see
     node_modules/cm-chessboard/src/extensions/accessibility/Accessibility.js's
     showFocusIndicator()) ship with NO fill/stroke of their own, so leaving
     them unstyled renders as a solid black square (found during Phase 7c's
     own visual QA, not a cosmetic nicety -- an unstyled .keyboard-focus
     rect fully occludes the piece under it). */
  .cm-chessboard-widget:focus-within { outline: var(--border-control) solid var(--color-focus); outline-offset: 2px; border-radius: var(--radius-sm); }
  .cm-chessboard .keyboard-focus-indicator .keyboard-focus { fill: none; stroke: var(--color-focus); stroke-width: 3px; pointer-events: none; }
  .cm-chessboard .keyboard-focus-indicator .keyboard-from-square { fill: var(--color-hover); stroke: var(--color-focus); stroke-width: 2px; pointer-events: none; }
  .cm-chessboard-accessibility.visually-hidden {
    position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px;
    overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0;
  }
  .cm-chessboard-accessibility button:disabled,
  .cm-chessboard-accessibility button:disabled:hover { opacity: 0.5; cursor: not-allowed; pointer-events: none; }
  .board-replay-controls { display: flex; gap: var(--space-2); margin: var(--space-3) 0 0; }
  .board-replay-controls button {
    min-height: 44px; min-width: 44px; font: inherit; font-weight: var(--weight-bold);
    padding: var(--space-2) var(--space-3); border: 1.5px solid var(--color-border-strong);
    border-radius: var(--radius-md); background: var(--color-surface); cursor: pointer;
  }
  .board-replay-controls button:hover:not(:disabled) { background: var(--color-hover); }
  .board-replay-controls button:disabled { opacity: 0.45; cursor: not-allowed; }

  /* T3: the interactive ECO explorer (Phase 7e). Board + paste tools in a
     fixed-max-width left column, the search/results panel filling the
     remainder -- same two-column pattern as the drill page's
     .drill-layout, reused rather than inventing a second one
     (design-standards.md: conform at the interaction layer). Single
     column below 1024px. */
  .explorer-layout { margin: var(--space-4) 0 var(--space-6); }
  @media (min-width: 1024px) {
    .explorer-layout {
      display: grid;
      grid-template-columns: minmax(0, 420px) 1fr;
      gap: var(--space-6);
      align-items: start;
    }
  }
  main .explorer-layout, main .explorer-top-families { max-width: none; }

  .explorer-board-mount { min-height: 320px; }
  .explorer-current-line { font-weight: var(--weight-bold); margin: var(--space-3) 0 0; min-height: 1.4em; }
  .explorer-identify-status { color: var(--color-muted); font-size: var(--text-sm); margin: var(--space-1) 0 var(--space-3); min-height: 1.2em; }

  .explorer-paste { border: 1px solid var(--color-border); border-radius: var(--radius-md); padding: var(--space-3) var(--space-4); margin-top: var(--space-3); }
  .explorer-paste summary { cursor: pointer; font-weight: var(--weight-bold); min-height: 24px; }
  .explorer-paste-inputs { display: flex; flex-direction: column; gap: var(--space-2); margin-top: var(--space-3); }
  .explorer-paste-inputs label { font-size: var(--text-sm); font-weight: var(--weight-bold); }
  .explorer-paste-row { display: flex; gap: var(--space-2); }
  .explorer-paste-inputs input,
  .explorer-paste-inputs textarea {
    font: inherit; padding: var(--space-2) var(--space-3); border: 1.5px solid var(--color-border-strong);
    border-radius: var(--radius-md); background: var(--color-surface); color: var(--color-text); width: 100%;
  }
  .explorer-paste-inputs input:focus-visible,
  .explorer-paste-inputs textarea:focus-visible { outline: 3px solid var(--color-focus); outline-offset: 2px; }
  .explorer-paste-inputs textarea { resize: vertical; font-size: var(--text-sm); }
  .explorer-paste-row button,
  .explorer-paste-inputs > button {
    min-height: 44px; font: inherit; font-weight: var(--weight-bold); white-space: nowrap;
    padding: var(--space-2) var(--space-4); border: 1.5px solid var(--color-border-strong);
    border-radius: var(--radius-md); background: var(--color-surface); cursor: pointer;
  }
  .explorer-paste-row button:hover,
  .explorer-paste-inputs > button:hover { background: var(--color-hover); }
  .explorer-error { color: var(--color-loss-text); font-size: var(--text-sm); min-height: 1.2em; margin: var(--space-2) 0 0; }

  .explorer-search-panel .lookup-form { margin-bottom: var(--space-2); }
  .explorer-result-count { color: var(--color-muted); font-size: var(--text-sm); min-height: 1.2em; margin: 0 0 var(--space-2); }
  .explorer-result-name {
    font: inherit; font-weight: var(--weight-bold); color: var(--color-accent-dark); background: none;
    border: none; padding: 0; min-height: 24px; cursor: pointer; text-decoration: underline;
  }
  .explorer-result-name:hover { color: var(--color-accent); }
  .explorer-moves-cell { font-size: var(--text-sm); color: var(--color-muted); }
  .explorer-top-families { list-style: none; padding: 0; display: grid; gap: var(--space-2); grid-template-columns: repeat(auto-fill, minmax(240px, 1fr)); }
  .explorer-top-families li { border: 1px solid var(--color-border); border-radius: var(--radius-md); padding: var(--space-3); }
  .explorer-family-meta { display: block; color: var(--color-muted); font-size: var(--text-xs); margin-top: var(--space-1); }
  .explorer-noscript { border: 1px solid var(--color-border-strong); border-radius: var(--radius-md); padding: var(--space-4); }

  @media (max-width: 640px) {
    body { padding: var(--space-4) var(--space-3) var(--space-6); }
    h1.page-title { font-size: var(--text-xl); }
    .table-hint { display: block; }
    .wdl-bar { width: 72px; }
    /* Keep the hero table's widened bar filling its cell even on mobile,
       rather than shrinking to the compact 72px default above; same
       specificity, so declaration order (this rule after .wdl-bar) decides. */
    .wdl-bar--lg { width: 100%; min-width: 0; }
    .rep-node-row { padding: var(--space-2) var(--space-3); }
    .lookup-form { flex-direction: column; align-items: stretch; }
    .lookup-form input, .lookup-form select { flex: 1 1 auto; width: 100%; }
    .lookup-form label { width: 100%; }
    .card-grid { grid-template-columns: 1fr; }
  }
`,FAVICON_DATA_URI="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 64 64'%3E%3Ccircle cx='32' cy='32' r='30' fill='%232a4d3a'/%3E%3Ccircle cx='32' cy='19' r='7' fill='%23f8f5ea'/%3E%3Crect x='26' y='24' width='12' height='10' fill='%23f8f5ea'/%3E%3Crect x='19' y='33' width='26' height='4' rx='2' fill='%23f8f5ea'/%3E%3Cpath d='M20 37L44 37L48 50L16 50Z' fill='%23f8f5ea'/%3E%3Crect x='15' y='50' width='34' height='8' rx='3' fill='%23f8f5ea'/%3E%3C/svg%3E",OG_DEFAULT_IMAGE="https://repertoire-builder.com/og-default.png";function renderDocumentHead(arg){let opts=typeof arg=="string"?{title:arg}:arg||{},{title,description,canonical,ogType="website",jsonLd,noindex,extraCss,feedUrl}=opts,metaDescription=description?`
  <meta name="description" content="${escapeHtml(description)}">`:"",canonicalLink=canonical?`
  <link rel="canonical" href="${escapeHtml(canonical)}">`:"",robotsMeta=noindex?`
  <meta name="robots" content="noindex">`:"",og=`
  <meta property="og:title" content="${escapeHtml(title)}">`+(description?`
  <meta property="og:description" content="${escapeHtml(description)}">`:"")+(canonical?`
  <meta property="og:url" content="${escapeHtml(canonical)}">`:"")+`
  <meta property="og:type" content="${escapeHtml(ogType)}">
  <meta property="og:site_name" content="Repertoire Builder">
  <meta property="og:image" content="${escapeHtml(OG_DEFAULT_IMAGE)}">
  <meta property="og:image:width" content="1200">
  <meta property="og:image:height" content="630">
  <meta name="twitter:card" content="summary_large_image">`,jsonLdBlock=jsonLd?`
  ${jsonLd}`:"",extraStyleBlock=extraCss?`
  <style>${extraCss}</style>`:"",feedLink=feedUrl?`
  <link rel="alternate" type="application/rss+xml" title="Repertoire Builder \u2014 new opening guides and articles" href="${escapeHtml(feedUrl)}">`:"";return`<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>${escapeHtml(title)}</title>${metaDescription}${canonicalLink}${robotsMeta}${og}${feedLink}
  <link rel="icon" href="${FAVICON_DATA_URI}">
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">
  <link rel="preload" href="/fonts/fraunces-variable.woff2" as="font" type="font/woff2" crossorigin>
  <style>${SITE_CSS}</style>${extraStyleBlock}${jsonLdBlock}
  <script data-goatcounter="https://dylangerrrr.goatcounter.com/count" async src="//gc.zgo.at/count.js"><\/script>
  <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-9767914878112531" crossorigin="anonymous"><\/script>
</head>`}var NAV_ORDER=["repertoire","openings","eco","drill","guides","faq","player"],NAV_LABELS={repertoire:"Repertoire explorer",openings:"Openings",eco:"ECO index",drill:"Opening drill",guides:"Guides",faq:"FAQ",player:"Player lookup"};function renderHeader(nav,active=null){let links=NAV_ORDER.filter(key=>nav[key]!=null).map(key=>`<a href="${escapeHtml(nav[key])}"${active===key?' aria-current="page"':""}>${escapeHtml(NAV_LABELS[key])}</a>`).join(`
      `);return`<header class="site-header">
    <a class="brand" href="${escapeHtml(nav.home||nav.repertoire||"/")}"><span class="brand-mark" aria-hidden="true">&#9822;</span>Repertoire Builder</a>
    <nav class="site-nav" aria-label="Main">
      ${links}
    </nav>
  </header>`}var KOFI_URL="https://ko-fi.com/flavaa",BMC_URL="https://buymeacoffee.com/dylanger254",NEWSLETTER_FORM_ACTION=null,NEWSLETTER_FORM_METHOD="POST";function renderNewsletterSignup(){return NEWSLETTER_FORM_ACTION?`<form class="newsletter-signup" action="${escapeHtml(NEWSLETTER_FORM_ACTION)}" method="${escapeHtml(NEWSLETTER_FORM_METHOD)}">
    <h2 class="newsletter-heading">Get new openings and guides by email</h2>
    <p class="newsletter-description">One email when a new opening page or guide ships. No spam, unsubscribe anytime.</p>
    <div class="newsletter-fields">
      <label for="newsletter-email" class="sr-only">Email address</label>
      <input id="newsletter-email" name="email" type="email" required placeholder="you@example.com" autocomplete="email">
      <button type="submit">Subscribe</button>
    </div>
  </form>`:`<div class="newsletter-signup newsletter-signup--pending">
    <h2 class="newsletter-heading">Get new openings and guides by email</h2>
    <p class="newsletter-description">Email sign-up isn&rsquo;t live yet &mdash; check back soon, or follow the <a href="feed.xml">RSS feed</a> in the meantime.</p>
  </div>`}function renderDisclosure(){return'<p class="disclosure-note">Disclosure: this site includes voluntary support links (Ko-fi, Buy Me a Coffee) and may in the future include affiliate links that earn a small commission on qualifying purchases at no extra cost to you. Support and affiliate links never influence the win-rate data, rankings, or analysis shown on this site &mdash; all of that comes directly from Lichess&rsquo;s public API and Opening Explorer, unaffected by any link on this page.</p>'}function renderFooter(innerHtml,legalLinks){let legalRow=legalLinks?`
  <nav class="legal-links" aria-label="Legal">
    ${legalLinks.privacy?`<a href="${escapeHtml(legalLinks.privacy)}">Privacy policy</a>`:""}
    ${legalLinks.about?`<a href="${escapeHtml(legalLinks.about)}">About</a>`:""}
    ${legalLinks.contact?`<a href="${escapeHtml(legalLinks.contact)}">Contact</a>`:""}
  </nav>`:"";return`<footer class="site-footer">${innerHtml}
  ${renderNewsletterSignup()}
  <div class="support-links">
    <a href="${KOFI_URL}" target="_blank" rel="noopener noreferrer">&#9749; Support on Ko-fi</a>
    <a href="${BMC_URL}" target="_blank" rel="noopener noreferrer">&#9749; Buy Me a Coffee</a>
  </div>
  ${renderDisclosure()}${legalRow}</footer>`}function renderPageHead({breadcrumb="",eyebrow="",title,subtitle="",meta=""}){return`${breadcrumb}
    ${eyebrow?`<p class="page-eyebrow">${eyebrow}</p>`:""}
    <h1 class="page-title">${title}</h1>
    ${subtitle?`<p class="subtitle">${subtitle}</p>`:""}
    ${meta}`}function wrapTable(tableHtml){return`
    <p class="table-hint">Scroll to see more &rarr;</p>
    <div class="table-scroll" tabindex="0" role="region" aria-label="Scrollable data table">${tableHtml}</div>`}function deltaClassFor(change){return change==null?"delta--zero":change>0?"delta--pos":change<0?"delta--neg":"delta--zero"}function renderRatingTable(ratingRows){if(ratingRows.length===0)return'<p class="empty-note">No rating history found.</p>';let rows=ratingRows.map(row=>{var _a,_b,_c;let deltaText=row.change==null?"-":`${row.change>=0?"+":""}${row.change}`;return`
      <tr>
        <td>${escapeHtml(row.variant)}</td>
        <td class="num">${(_a=row.current)!=null?_a:"-"}</td>
        <td class="num">${(_b=row.peak)!=null?_b:"-"}</td>
        <td class="num">${(_c=row.low)!=null?_c:"-"}</td>
        <td class="delta num ${deltaClassFor(row.change)}">${deltaText}</td>
        <td class="num">${row.gamesRecorded}</td>
      </tr>`}).join("");return wrapTable(`
    <table>
      <thead>
        <tr><th>Variant</th><th class="num">Current</th><th class="num">Peak</th><th class="num">Low</th><th class="num">Change</th><th class="num">Data points</th></tr>
      </thead>
      <tbody>${rows}</tbody>
    </table>`)}function resultBadge(result){let label=result==="win"?"Win":result==="loss"?"Loss":"Draw";return`<span class="badge badge--${escapeHtml(result)}">${label}</span>`}function renderGamesTable(gameSummary){var _a;if(gameSummary.totalGames===0)return'<p class="empty-note">No recent games found.</p>';let rows=gameSummary.results.map(r=>{var _a2;return`
      <tr class="result-${escapeHtml(r.result)}">
        <td>${escapeHtml(r.date||"-")}</td>
        <td>${escapeHtml(r.opponent)}</td>
        <td class="num">${escapeHtml((_a2=r.opponentRating)!=null?_a2:"-")}</td>
        <td>${escapeHtml(r.color)}</td>
        <td>${escapeHtml(r.variant)} / ${escapeHtml(r.speed)}</td>
        <td>${resultBadge(r.result)}</td>
      </tr>`}).join("");return`
    <p class="summary-line">${gameSummary.wins}W / ${gameSummary.losses}L / ${gameSummary.draws}D
       out of ${gameSummary.totalGames} games (win rate ${formatPct(gameSummary.winRate)}%,
       avg opponent rating ${(_a=gameSummary.avgOpponentRating)!=null?_a:"n/a"})</p>`+wrapTable(`
    <table>
      <thead>
        <tr><th>Date</th><th>Opponent</th><th class="num">Opp. rating</th><th>Color</th><th>Variant/Speed</th><th>Result</th></tr>
      </thead>
      <tbody>${rows}</tbody>
    </table>`)}function renderPlayerPage({username,ratingRows,gameSummary,nav={player:"/",repertoire:"/repertoire"}}){return`<!DOCTYPE html>
<html lang="en">
${renderDocumentHead(`${username} | Repertoire Builder`)}
<body class="layout--wide">
  ${renderHeader(nav,"player")}
  <main>
    <h1 class="page-title">${escapeHtml(username)}</h1>
    <p class="subtitle">Lichess rating history and recent games</p>

    <h2>Ratings by variant</h2>
    ${renderRatingTable(ratingRows)}

    <h2>Recent games</h2>
    ${renderGamesTable(gameSummary)}
  </main>
  ${renderFooter('Data source: <a href="https://lichess.org/api">lichess.org/api</a>.')}
</body>
</html>
`}function renderRepertoireNode(node){let winPct=typeof node.winPct=="number"?node.winPct:null,drawPct=typeof node.drawPct=="number"?node.drawPct:null,lossPct=typeof node.lossPct=="number"?node.lossPct:null,ratingNote=node.averageRating?`<span class="rep-rating">avg rating ${node.averageRating}</span>`:"",children=node.children&&node.children.length>0?`<ul>${node.children.map(renderRepertoireNode).join("")}</ul>`:"",wdlTitle=`${node.mover} win/draw/loss: ${formatPct(winPct)}% / ${formatPct(drawPct)}% / ${formatPct(lossPct)}%`,wdlBar=winPct==null?"":`<span class="wdl-bar" title="${escapeHtml(wdlTitle)}">
        <span class="wdl-seg--win" style="width:${winPct}%"></span>
        <span class="wdl-seg--draw" style="width:${drawPct}%"></span>
        <span class="wdl-seg--loss" style="width:${lossPct}%"></span>
      </span>
      <span class="wdl-label">${formatPct(winPct)}% / ${formatPct(drawPct)}% / ${formatPct(lossPct)}%</span>`;return`
    <li>
      <div class="rep-node-row">
        <span class="move-chip move-chip--${escapeHtml(node.mover)}">${escapeHtml(node.san)}</span>
        <span class="rep-games">${node.games.toLocaleString()} games <span class="rep-pct">(${formatPct(node.playedPct)}% of this position)</span></span>
        ${wdlBar}
        ${ratingNote}
      </div>
      ${children}
    </li>`}function renderRepertoireTree(tree){return!Array.isArray(tree)||tree.length===0?'<p class="empty-note">No repertoire data found for this rating band and color.</p>':`<ul class="repertoire-tree">${tree.map(renderRepertoireNode).join("")}</ul>`}function renderRepertoirePage({ratingBand,color,opening,totals,tree,nav={player:"/",repertoire:"/repertoire"},legalLinks,canonical,description}){let totalGames=totals?totals.white+totals.draws+totals.black:null,openingNote=opening?` &mdash; starting from ${escapeHtml(opening.name)} (${escapeHtml(opening.eco)})`:"",totalsNote=totals?`<p class="summary-line">${totalGames.toLocaleString()} games played from the starting position in this rating band
        (${totals.white.toLocaleString()}W / ${totals.draws.toLocaleString()}D / ${totals.black.toLocaleString()}L).</p>`:"",title=`Opening repertoire explorer (${ratingBand}, ${color}) | Repertoire Builder`;return`<!DOCTYPE html>
<html lang="en">
${renderDocumentHead({title,description,canonical})}
<body class="layout--wide">
  ${renderHeader(nav,"repertoire")}
  <main>
    ${renderPageHead({eyebrow:"Repertoire",title:"Opening repertoire explorer",subtitle:`Rating band ${escapeHtml(ratingBand)}, playing as ${escapeHtml(color)}${openingNote}`,meta:totalsNote})}
    <p class="repertoire-intro">Most-played moves at each ply for players in this rating band, with win/draw/loss rates per move.
       Your color's plies show the top choices actually played at this rating; the opponent's replies show
       only their single most common response, to keep the tree readable.</p>
    ${renderRepertoireTree(tree)}
  </main>
  ${renderFooter('Data source: <a href="https://lichess.org/api#tag/Opening-Explorer">Lichess Opening Explorer API</a> (explorer.lichess.ovh, keyless, no account required).',legalLinks)}
</body>
</html>
`}module.exports={renderPlayerPage,renderRepertoireTree,renderRepertoirePage,renderGamesTable,renderRatingTable,escapeHtml,formatPct,SITE_CSS,FAVICON_DATA_URI,DESIGN_TOKENS,renderDocumentHead,renderHeader,renderFooter,renderDisclosure,renderNewsletterSignup,renderPageHead,wrapTable,NAV_ORDER,NAV_LABELS}}});var require_playerLookup_client=__commonJS({"src/browser/playerLookup.client.js"(){var{fetchRatingHistory,fetchRecentGames,LichessNotFoundError,LichessRateLimitError,LichessApiError}=require_fetchLichess(),{summarizeRatingHistory,summarizeGames}=require_process(),{renderRatingTable,renderGamesTable,escapeHtml}=require_render();(function(){function $(selector){return document.querySelector(selector)}function setStatus(html){$("#result").innerHTML=html}async function lookupPlayer(username){setStatus('<p class="status-message status-message--loading">Loading&hellip;</p>');try{let[history,games]=await Promise.all([fetchRatingHistory(username),fetchRecentGames(username,{max:15})]),ratingRows=summarizeRatingHistory(history),gameSummary=summarizeGames(games,username);setStatus(`
        <h2>${escapeHtml(username)}</h2>
        <h3>Ratings by variant</h3>
        ${renderRatingTable(ratingRows)}
        <h3>Recent games</h3>
        ${renderGamesTable(gameSummary)}
      `)}catch(err){err instanceof LichessNotFoundError?setStatus(`<p class="status-message status-message--error">No such Lichess user: ${escapeHtml(username)}</p>`):err instanceof LichessRateLimitError?setStatus('<p class="status-message status-message--error">Lichess API rate limit hit, try again shortly.</p>'):err instanceof LichessApiError?setStatus(`<p class="status-message status-message--error">Lichess API request failed: ${escapeHtml(err.message)}</p>`):err instanceof TypeError?setStatus(`<p class="status-message status-message--error">Could not reach the Lichess API from this page (network error or the browser blocked the cross-origin request). Details: ${escapeHtml(err.message)}</p>`):setStatus(`<p class="status-message status-message--error">Lookup failed: ${escapeHtml(err.message)}</p>`)}}function init(){let form=$("#lookup-form");if(!form)return;form.addEventListener("submit",event=>{event.preventDefault();let username=$("#username").value.trim();if(!username)return;let url=new URL(window.location.href);url.searchParams.set("username",username),window.history.replaceState(null,"",url),lookupPlayer(username)});let prefill=new URLSearchParams(window.location.search).get("username");prefill&&($("#username").value=prefill,lookupPlayer(prefill))}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",init):init()})()}});require_playerLookup_client();})();
